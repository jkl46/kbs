package gui.screens;

import javax.swing.*;

public class Archief extends JDialog {
    public Archief(JFrame frame, boolean modal) {
        super(frame, modal);
        setSize(600, 600);
        setTitle("Archief");
        setVisible(true);
    }
}
